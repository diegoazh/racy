{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "description": "racy library is a group of packages to work with serverless framework in different clouds",
  "main": "index.js",
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap",
    "prepare": "husky install",
    "clean": "npx lerna run clean && rimraf ./**/**/package-lock.json && rimraf ./**/**/node_modules && rimraf ./node_modules && rimraf ./package-lock.json"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/diegoazh/racy.git"
  },
  "keywords": [
    "racy",
    "serverless",
    "aws",
    "azure"
  ],
  "author": "Diego A. Zapata Häntsch",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/diegoazh/racy/issues"
  },
  "homepage": "https://github.com/diegoazh/racy#readme",
  "devDependencies": {
    "@commitlint/cli": "^17.1.2",
    "@commitlint/config-conventional": "^17.1.0",
    "@commitlint/config-lerna-scopes": "^17.0.2",
    "husky": "^8.0.1",
    "jest": "^29.0.3",
    "lerna": "^5.5.2"
  }
}
